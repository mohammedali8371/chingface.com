# ================================================================
# ملف requirements.txt
# ---------------------
# هذا الملف يحدد جميع المكتبات المطلوبة للمشروع مع إصداراتها.
# جميع الإصدارات المختارة متوافقة مع Python 3.11.
#
# طريقة التثبيت:
#   pip install -r requirements.txt
#
# ملاحظات مهمة:
#   1. insightface 0.7.3 لا يعمل مع numpy 2.x لذلك قمنا بتثبيت 1.26.4.
#   2. نستخدم opencv-python-headless (بدون واجهة رسومية) لتقليل
#      حجم التثبيت ويعمل بسلاسة على الخوادم (Render).
#   3. GFPGAN تحسين اختياري ولا يتم تثبيته هنا (يتطلب torch ضخمة).
#      يمكنك تثبيته يدوياً على خادمك إن رغبت: pip install gfpgan
# ================================================================

# ---------- إطار العمل والخادم ----------
# مكتبة تيليجرام الرسمية (v22+) - تدعم async فقط
python-telegram-bot~=22.0

# إطار عمل الويب للـ API و Webhook
fastapi~=0.115.0

# خادم ASGI لتشغيل التطبيق
uvicorn[standard]~=0.30.0

# قراءة ملفات .env
python-dotenv~=1.0.1

# مكتبة الملفات المرفوعة (Form parsing) لـ FastAPI
python-multipart~=0.0.9

# ---------- قاعدة البيانات ----------
# قاعدة بيانات SQLite بشكل غير متزامن (async)
aiosqlite~=0.20.0

# ---------- التحقق من البيانات (Pydantic) ----------
pydantic~=2.8.0
pydantic-settings~=2.4.0

# ---------- الذكاء الاصطناعي ومعالجة الصور ----------
# الحسابات الرقمية (مثبتة على 1.26.4 للتوافق مع insightface)
numpy==1.26.4

# مكتبة معالجة الصور والفيديو (نسخة headless للخوادم)
opencv-python-headless==4.9.0.80

# مكتبة اكتشاف وتحليل الوجوه (InsightFace)
insightface==0.7.3

# محرك تشغيل نماذج التعلم العميق على المعالج (CPU)
onnxruntime==1.17.1

# مكتبة تنسيقات النماذج (ONNX)
onnx==1.16.1

# الحسابات العلمية (تابع تلقائي لـ insightface)
scipy~=1.13.0

# شريط التقدم عند تحميل النماذج
tqdm~=4.66.0

# طلبات HTTP (تابع لـ insightface لتحميل النماذج)
requests~=2.32.0

# ---------- مراقبة النظام ----------
# لقراءة استهلاك الذاكرة والمعالج والإحصائيات
psutil~=5.9.8
