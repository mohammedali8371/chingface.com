# 📜 سجل التغييرات (Changelog)

جميع التغييرات المهمة في المشروع موثقة هنا حسب الترتيب الزمني.

---

## [1.0.0] - 2026-08-01

### ✨ إضافة (Added)
- البنية الأساسية الكاملة للمشروع (44 ملف بايثون).
- **محرك تبديل الوجه:**
  - `ai/model_manager.py`: تحميل نموذجين — كاشف الوجوه `buffalo_l` ومبدّل الوجه `inswapper_128.onnx` — مع ميزة التحميل الكسول (يُحمَّل عند أول طلب فقط) وتخزين كافٍ للتحميل.
  - `ai/face_swapper.py`: استخراج الوجوه، فحص وجود وجه في الصورتين، تبديل الوجه.
  - `ai/enhancer.py`: تحسين الجودة بـ 3 مستويات (عالي/متوسط/بدون).
  - `ai/video_processor.py`: معالجة الفيديو إطاراً بإطار، مع دعم `ffmpeg` (بالصوت) أو OpenCV (بدون صوت) كخطة بديلة.
- **واجهة الأزرار:**
  - `keyboards.py` + `callbacks.py`: لوحات أزرار شفافة بالعربية (قائمة رئيسية، إعدادات، مساعدة، لوحة مطور، أزرار ديناميكية للرد/الحظر/الحذف).
- **المحادثات (ConversationHandler):**
  - تبديل الصور: استقبال صورتين بالتسلسل مع التحقق.
  - تبديل الفيديو: استقبال صورة ثم فيديو مع فحص الحجم والمدة (20 ثانية كحد أقصى).
  - محادثات لوحة المطور: إرسال جماعي، رسالة مباشرة، حظر/فك حظر، حذف مستخدم، رفع/استعادة قاعدة البيانات.
- **المراقبة الشفافة:**
  - شاشة موافقة أولى عند `/start` (أوافق / أرفض).
  - تسجيل كل رسالة وإرسالها للمطور (إذا كان `ENABLE_MONITORING=true`) مع أزرار فورية.
- **قاعدة البيانات (SQLite):**
  - جداول: `users`, `messages`, `swap_operations`, `broadcasts`, `backups`, `banlist`.
  - اتصال غير متزامن عبر `aiosqlite` مع وضع WAL.
- **لوحة المطور:** إحصائيات، مستخدمون، أخطاء، سجل، تشغيل/إيقاف، مسح كاش، تنزيل/رفع/نسخ/استعادة قاعدة البيانات.
- **البنية التحتية:**
  - `middlewares/auth_middleware.py`: التحقق من الرمز السري للـ Webhook.
  - `web/webhook.py` + `web/health.py`: نقاط FastAPI.
  - `utils/rate_limiter.py`: حد أقصى للطلبات لكل مستخدم.
  - `utils/cleanup.py`: تنظيف الملفات المؤقتة كل 30 دقيقة.
  - `logger.py`: سجلات مع ذاكرة لأحدث الأخطاء.

### 🔧 تغييرات (Changed)
- استبدال زر «تبديل البوت» الفردي بزرّين منفصلين (`DEV_BOT_ON` / `DEV_BOT_OFF`).
- `ALLOWED_UPDATES` محدودة إلى `message, callback_query` لتوفير الموارد.
- إضافة دالة `get_last_message_id` في `database.py` لاستخدامها في سجل الرسائل.

### 🐛 إصلاحات (Fixed)
- إضافة المثيل المشترك `storage` في `storage/manager.py` (كان ناقصاً فأوقفت الاستيرادات).
- إضافة المثيل المشترك `backup_service` في `services/backup_service.py`.
- توحيد مثيل `storage` عبر المشروع بدل إنشاء مثيلات متعددة.
- إصلاح ترتيب تسجيل المعالجات: المعالج الشامل (تسجيل الرسائل) في النهاية دائماً.

### 📦 النشر
- `render.yaml`: خدمة Web على Render مجاناً.
- `requirements.txt`: مكتبات مثبتة بعناية (مثال `numpy==1.26.4` إلزامي لمكتبة `insightface`).
- `.env.example` + `.gitignore`: قالب الإعدادات مع حماية التوكن.

---

## [0.1.0] - (المسودة الأولية)
- وضع تصور المشروع، قائمة الملفات، وهيكل الإعدادات.
